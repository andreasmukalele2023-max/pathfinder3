[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# No manual redirects here: the Netlify build target generates dist/_redirects
# and .netlify/functions-internal/server so every route is server-rendered.
